# REVIEW.md

project001におけるコードレビューの方針・手順を定める。レビュー専用のルールはこのファイルに集約し、開発ルールはAGENTS.md/CLAUDE.mdに残す（重複記載しない）。reviewer Agent（`.claude/agents/reviewer.md`）は本ファイルに従う。

## 基本姿勢: 敵対的検証（Adversarial Review）

レビュー担当は実装者を支援する立場ではなく、品質保証・監査・ペネトレーションテスターの立場で振る舞う。「正しい理由」ではなく「壊れる理由」「失敗する理由」「仕様を満たさない理由」を優先して探す。「問題が必ず存在する」という前提から出発し、検証を経るまで肯定的な結論を出さない。実装者の意図を推測して擁護しない。

利用可能なCapability（docs/capability-layer.md参照）があれば優先利用する。現在Tier1のCapabilityは無く、Agent-Reach/Code Review Graph/Context7はTier2（検出のみ、振る舞い未統合）のため、指摘の裏付けはRead/Grep/Bashによる調査で行う。

以下の表現は、十分な検証を経ずには使用しない: 「問題ありません」「良い実装です」「特にありません」。

## レビュー観点

該当する観点をすべて確認する。該当しない観点は、無理に指摘を作らず簡潔に非該当と記す（存在しない問題を捏造しない。project001自体はアプリケーションコードを持たないため、UX/レスポンシブ/ブラウザ互換性/非同期処理等は多くの場合非該当になる。個別アプリのリポジトリでは全観点が該当し得る）。

- 要件との不一致
- ロジックエラー
- 境界値
- エッジケース
- 回帰（既存機能への影響）
- セキュリティ
- パフォーマンス
- 保守性
- 可読性
- 拡張性
- UX
- レスポンシブ
- アクセシビリティ
- データ整合性
- 状態管理
- エラーハンドリング
- 非同期処理
- ブラウザ互換性
- 依存関係（呼び出し元・呼び出し先への影響、blast radius）

## レビュー手順

1. 要件を再確認する
2. 実装内容を確認する
3. 要件との差分を抽出する
4. 壊れるケースを列挙する（仮説段階。網羅的に、否定せず出す）
5. **検証パス**: 各仮説について再現手順を考え、実際にコード・挙動（必要なら`Bash`でテスト・grep等）を確認して真偽を判定する
6. 検証で裏付けが取れた指摘のみ、修正案とともに提示する
7. 修正後、同じ観点で再レビューする

### 検証パスの判定

手順4の仮説はそのまま報告せず、手順5で以下いずれかを付与してから報告する。

- `CONFIRMED`: 実際に壊れる/仕様を満たさないことをコード・挙動で確認した
- `PLAUSIBLE`: 高い疑いはあるが、実行環境やデータ依存等の理由で完全には確認しきれない

裏付けが取れず、可能性も低いと判断した仮説は報告しない（誤検出防止の「捏造の禁止」。「肯定的結論の安易な使用禁止」と対になる）。

検証で裏付けが取れた指摘（CONFIRMED/PLAUSIBLEのいずれか）でも、次のいずれかに該当し正確性・要件充足・セキュリティ・データ整合性に影響しないものは、`Low`/`Nit`（重要度の目安参照）として提示し必須修正とはしない（過剰検出防止のルール。捏造の禁止と対になる）。セキュリティ・データ整合性に関わる指摘はこの分類の対象外とし、必須側に残す。

- 将来の拡張性のみを根拠とする一般化要求
- 要件範囲外のリファクタリング提案
- スタイルの好み

敵対的検証は「壊れる理由を見逃さない」ためのものであり、指摘件数を増やすためのものではない。件数を目的にした指摘は、AGENTS.md設計原則5（投機的な抽象化・間接層を追加しない）に反する過剰実装をDeveloperに強いる。

呼び出し元・影響ファイルの検証、変更内容の要約確認はBash/grepによる確認で行う（Code Review Graphは現在Tier2のため組み込まない。Tier1昇格後の利用方法はdocs/code-review-graph.md参照）。

## 指摘の記載形式

指摘ごとに以下を記載する。重要度の高い順に並べる。

- **重要度**: Critical / High / Medium / Low / Nit
- **該当箇所**: `ファイルパス:行番号`
- **問題**: 何が問題か
- **根本原因**: なぜ起きるか
- **影響範囲**: どこまで影響するか
- **再現条件**: どのような入力・状況で発生するか
- **推奨修正**: 具体的な直し方
- **判定**: CONFIRMED / PLAUSIBLE

## 重要度の目安

- **Critical**: データ損失、セキュリティ侵害、本番停止に直結する
- **High**: 主要な要件を満たさない、または高頻度で発生する不具合
- **Medium**: 限定的な条件で発生する不具合、保守性を明確に損なう設計
- **Low**: 軽微な不具合、改善余地のある実装
- **Nit**: スタイル・命名等、動作に影響しない指摘

## レビュー完了条件

以下をすべて満たすまでレビューを終了しない。

- 要件を満たしている
- 回帰がない
- エッジケースを確認済み
- 実装と設計が一致している
- テスト観点を確認済み

満たさない場合はManagerへ差し戻し理由を返す。ManagerがDeveloperへ差し戻す（修正ループはAGENTS.md参照）。承認するまでDeveloper→Reviewerを繰り返す。
