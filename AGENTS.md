# AGENTS.md

project001は、新規アプリ開発に共通する開発方針・設計原則・ワークフロー・レビュー基準を提供するAI開発エージェントテンプレートである。個別アプリの実装は、このテンプレートから作成した別リポジトリで行う。すべてのAIエージェント（Claude Code含む）は本ファイルの内容を最優先で守る。Claude Code固有の設定・運用は CLAUDE.md を参照。

## 設計原則

1. **理解してから作る**: 実装前に問題とコードの流れを理解する。理解を伴わない最小差分は誤った修正になりかねない。
2. **判定ラダー**: コードを書く前に上から順に確認し、最初に当てはまった段階で止める。
   1. そもそも必要か（YAGNI）
   2. 既存コードベースに同等の実装があるか
   3. 標準ライブラリ・既存の成熟した外部ライブラリで足りるか（「ない」と決めつける前にドキュメント・型定義を確認する）
   4. プラットフォームのネイティブ機能で足りるか
   5. インストール済みの依存関係で解決できるか（新規パッケージ追加は避ける）
   6. 1行で書けるか
   7. それでも足りない場合のみ、要件を過不足なく満たす最小実装を書く
3. **後方互換性を維持しない**: 廃止するパスは互換レイヤー・フォールバック・マイグレーションではなく削除する。
4. **段階的に育てる**: 最小限で動くバージョンを作り、その上に積み増す。未完成の複雑さと引き換えに、動いているものを壊さない。
5. **責務は分離する。ただしファイルは増やさない**: 責務は明確に分けるが、新しいファイルや抽象化を作ることとは別問題。今日の要件を満たす最小構成にとどめ、投機的な抽象化・間接層は追加しない。
6. **最小 ≠ 一時しのぎ**: 要件を正しく理解した上での最小実装は長期的にも正しい実装である。「後で置き換える前提」の一時しのぎを黙って残さない。意図的に妥協する場合は `ponytail:` コメントで限界と改善方法を明記し、非自明なロジックには壊れたら失敗する最小限の確認（アサーション等）を1つ残す（自明な1行は不要）。
7. **バグは根本原因を直す**: 症状ではなく原因を直す。全呼び出し元を確認し、共有関数側を一度だけ直す。
8. **手を抜かない対象**: 問題の理解、信頼境界での入力検証、データ損失を防ぐエラーハンドリング、セキュリティ、アクセシビリティ、明示的に要求された事項。

出典: [DietrichGebert/ponytail](https://github.com/DietrichGebert/ponytail)（MIT License）をベースに統合。

## ワークフロー

User → Manager → Planner → Developer → Reviewer → Manager → Complete
（外部調査が必要な場合のみ、Planner→Researcher→Developerの間にResearcherが入る）

- **Manager**（Claude Codeのルートセッション自身）: タスク管理・Agent割当・優先順位判断・レビュー依頼・完了判定のみを行う。コードは直接書かない（docs/配下の状態更新は例外）。仕様を独断で変更せず、不明点はUserに確認する。
- **Researcher**: 外部情報の収集・重複除去・信頼性評価を行う。Plannerが必要と判断した場合のみManagerが起動する（常時起動しない）。利用可能な検索ツールがあれば優先利用し、なければ標準的な調査手段へ自動的に切り替える。
- **修正ループ**: Reviewerが問題を発見した場合、ManagerがDeveloperへ差し戻す。Reviewerが承認するまでDeveloper→Reviewerを繰り返す。
- Agentの起動はManagerのみが行う。Agent同士は互いを起動しない。
- Agent構成・モデル構成（Model Routing）・調査ワークフロー・Hook構成の詳細は docs/agents.md・docs/research-workflow.md を参照。

## レビュー基準（完了条件）

以下をすべて満たした場合のみ完了とする。

- 要件達成
- エラーなし
- 動作確認済み
- コードレビュー済み（上記「設計原則」に沿っているかを含む。過剰な抽象化・不要な依存追加・不要なボイラープレートがないか確認する）。レビューの姿勢・観点・手順・レビュー自体の完了条件は REVIEW.md（敵対的検証）に従う。上記4項目はタスク全体の完了条件、REVIEW.mdの完了条件はレビュー工程固有の下位条件である

## ドキュメント

- docs/tasks.md: 現在のタスク、優先順位、状態管理
- docs/progress.md: 作業履歴、次回開始位置
- docs/decisions.md: 設計判断とその理由
- docs/agents.md: Agent構成、モデル構成、オーケストレーション、Hook構成の詳細

新規プロジェクトとしてコピーした直後は、docs/のうちtasks.md/progress.md/decisions.mdの3ファイルからproject001自身の構築履歴をリセットする。手順はREADME.mdを参照（複製しない）。
